// Difference between '==' and '===' operators
// both are comparison operator .the difference between both the operator is that "==" is 
// used to compare both values and types.

var x='2';
var y=2;' ';
console.log(x==y);
console.log(x===y);